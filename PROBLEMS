add onClose function to free everything

libs/SDL/widgets/Text.h line 64:
u need to free the old text (u have and invalid size problem)
this will fix the small memory leak u're having (~+0.1MB\s)

FIXED###############################
libs/dataStructs/lists.h line 92
replace the int with void*
repalce %d with %p
add print Queue
add switch between print queue/ram
add userdata support
